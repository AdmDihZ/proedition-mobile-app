name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'  # Semanalmente aos domingos

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: 'Proedition Global Mu 33.1/MobileApp/package-lock.json'
        
    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '11'
        
    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
          
    - name: Install dependencies
      working-directory: 'Proedition Global Mu 33.1/MobileApp'
      run: npm ci
        
    - name: Build APK
      working-directory: 'Proedition Global Mu 33.1/MobileApp'
      run: |
        cd android
        ./gradlew assembleRelease
        cd ..
        
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: ProeditionMobileApp
        path: "Proedition Global Mu 33.1/MobileApp/android/app/build/outputs/apk/release/app-release.apk"
        
    - name: Create Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: "Proedition Global Mu 33.1/MobileApp/android/app/build/outputs/apk/release/app-release.apk"
        body: |
          🚀 **Proedition Global Mu - Mobile App v1.0.0**
          
          ## �� **Características:**
          - ✅ Sistema de autenticação completo
          - ✅ Chat global em tempo real
          - ✅ Interface de streaming para Mu Online
          - ✅ Sistema VIP integrado
          - ✅ Cash shop mobile
          - ✅ Perfil e configurações
          - ✅ Tema dark moderno
          - ✅ Suporte a 13 idiomas
          
          ## 🔧 **Instalação:**
          1. Baixe o APK
          2. Habilite "Fontes desconhecidas" no Android
          3. Instale o APK
          4. Abra o app e faça login
          
          ## 📞 **Suporte:**
          - Email: suporte@proedition.com
          - Discord: [Link do servidor]
          
          **�� Jogue Mu Online em qualquer lugar!**
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
